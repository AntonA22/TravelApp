import SwiftUI

extension Color {
    static let travelBlue = Color(red: 0.22, green: 0.45, blue: 1.0)
    static let travelBackground = Color(uiColor: .systemBackground)
    static let travelPrimary = Color(uiColor: .label)
    static let travelSecondary = Color(uiColor: .secondaryLabel)
    static let travelPlaceholder = Color(uiColor: .placeholderText)
    static let travelField = Color(uiColor: .secondarySystemBackground)
    static let travelCard = Color(uiColor: .tertiarySystemBackground)
    static let travelRouteCard = Color(uiColor: .systemGray5)
    static let travelSeparator = Color(uiColor: .separator)
}
